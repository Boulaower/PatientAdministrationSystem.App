import React from 'react'; // Import React for creating components.

const HomePage = () => {
  return (
    // Container div for the homepage content.
    <div>
      {/* Main heading for the homepage */}
      <h1>Welcome to the Patient Administration System</h1>
      {/* Instructional text for users on what they can do */}
      <p>Use the navigation to view and manage patients.</p>
    </div>
  );
};

export default HomePage; // Export the HomePage component for use in routing or as the main page.
